$('#newIllustrationForm').html("<%= escape_javascript(render(:partial=>"new",:locals => {:contest => @contest} ))%>");

$(document).ready(function() {
	$('#uploadModal').modal({
		show: true,
		keyboard: true
	}).on('shown.bs.modal', function () {
   			$("#confirm_form").modal('show');
		});
});
