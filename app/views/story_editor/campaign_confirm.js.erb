//window.location.replace("/contests/<%= @contest.id %>");
/*$("#campaign_popup").html("<%= escape_javascript(render(:partial=>"campaign_confirm"))%>");

$(document).ready(function() {
  $('#popup_campaign').modal({
	  show: true
	});
});
*/